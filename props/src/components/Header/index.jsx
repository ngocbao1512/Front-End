import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss'

Heading.propTypes = {
    
};

function Heading(props) {
    return (
        <div>
            Header
        </div>
    );
}

export default Heading;